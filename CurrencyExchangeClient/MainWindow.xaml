<Window x:Class="CurrencyExchangeClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Currency Exchange Client" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/app.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Currency Exchange Client" 
                   FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Server IP:" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="ServerIpTextBox" Text="127.0.0.1" Width="150" Margin="5,0"/>
            <TextBlock Text="Port:" Width="50" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox x:Name="ServerPortTextBox" Text="8888" Width="80" Margin="5,0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="From Currency:" Width="120" VerticalAlignment="Center"/>
            <ComboBox x:Name="FromCurrencyComboBox" Width="100" Margin="5,0">
                <ComboBoxItem Content="USD" IsSelected="True"/>
                <ComboBoxItem Content="EUR"/>
            </ComboBox>
            <TextBlock Text="To Currency:" Width="100" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <ComboBox x:Name="ToCurrencyComboBox" Width="100" Margin="5,0">
                <ComboBoxItem Content="EUR" IsSelected="True"/>
                <ComboBoxItem Content="USD"/>
            </ComboBox>
        </StackPanel>
        
        <Button Grid.Row="3" x:Name="GetRateButton" Content="Get Exchange Rate" 
                Click="GetRateButton_Click" Height="35" Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="4" Text="Result:" FontWeight="Bold" Margin="0,0,0,5"/>
        
        <TextBox Grid.Row="5" x:Name="ResultTextBox" IsReadOnly="True" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Background="#F5F5F5" Padding="10"/>
    </Grid>
</Window>

